# 报告模板数据更新（2026-09-11）

来源为用户提供的 `selfit-report-templates-2026-09-10.json`，导出时间 `2026-09-10T17:11:52.958Z`，SHA-256 为 `41a6725e1ae1f46c7de6d9d82f332b514586fc7fd2acf317896f43a4b131a389`。

## 更新内容

- 同步 24 份人格模板：16 份通用、4 份微胖、4 份男性。编辑器主数据、浏览器种子和前后端报告模板同步更新。
- 同步潮流趋势、约会、通勤 3 个合集的 12 条笔记标题、作者及来源链接；沿用独立灵感合集，不把场景名称当作人格代码。
- 修正导出中的男性性别标记和微胖模板身份冲突，继续使用项目既有模板 ID。
- 新增 8 张男性发型图及 1 张 EDGE 男性封面，经用户授权上传至既有七牛 `selfit` 私有空间。9 张图片均完成回读、SHA-256 和图片解码校验。
- 男性妆容的 8 张划线占位图不作为正式妆容参考展示，原始导出仍保留在本地处理记录。
- WABI 男性第 3、4 条笔记更新为“灰调阔腿”“头巾叠搭”，对应原图字节一致。FLOU 前两张导出图片是已处理原图的缩放版本，继续绑定已核验的原始素材。

## 数据兼容

112 套穿搭的 outfit ID、672 件单品、单品描述、原图绑定及穿着层次保持不变。已有候选素材库和默认穿搭推荐池保留。

编辑器种子版本为 18。`seedUpdateBaselines` 保存更新前的字段基线；浏览器中的字段仍与旧版一致时才同步新值。用户改写过的文案、颜色或整组素材保持原样。现有记录 ID 和当前选择保持不变，重复迁移不会产生副本。

导入脚本识别已经登记的素材内容地址，封面与卡片均可重新生成 asset ID 引用，无需再次上传。按标准导入流程重建全部 24 份报告，与本次结果一致。

## 验证与运行

- 相关 Python 测试共 76 项，覆盖笔记同步、男性拆款、灵感合集、报告穿搭、匹配和试穿预设；失败的旧数据假设已更新，相关用例复测通过。
- 相关 JavaScript 测试共 13 项通过，覆盖保存模板的迁移、男性与微胖版本、报告穿搭和灵感合集。
- 浏览器检查了 4 份男性模板的全部 9 张新图，并检查 EDGE 报告的桌面和 393px 手机预览：新图正确、男性妆容占位隐藏、无水平溢出或文字覆盖。延迟加载的穿搭图片随滚动显示正常。
- 浏览器检查工具记录到一次没有来源信息的 MutationObserver 错误；未观察到素材请求失败或报告渲染异常。
- `git diff --check` 通过。更新前已有的其他工作区改动保留。

处理记录、源文件、备份、上传清单和校验结果位于 `outputs/report-template-update-20260911/`。Git 中新增的 9 条素材记录只包含稳定对象地址和存储信息，临时签名保存在被忽略的 `material-assets.runtime.json` 中；本地已建立经过校验的图片缓存。

无缓存的新运行环境需按 [素材运行说明](SELFIT_STYLING_DELIVERY.md) 刷新独立运行时素材清单，并用 `SELFIT_MATERIAL_REGISTRY_PATH` 指向该文件。此次未发布服务器。
