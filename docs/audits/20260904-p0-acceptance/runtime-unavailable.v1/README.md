# 隔离运行：P0 内容不可用状态与关闭弹层焦点

本记录是局部诊断，不是完整 P0 验收通过证明。使用隔离目录 `/tmp/selfit-p0-runtime.VXfV0s`、本地 8011 服务、全新测试账户及明确标记的 synthetic MUTE 人格；没有运行色彩测试，没有复制共享服务的用户数据或密钥，没有生成试穿或发布。

## 结果

- 未配置获准的 P0 内容时，真实首页显示审核中提示，没有回退到未经批准的内容。
- 修复前连续 16 次 Tab 会进入已关闭的上传、会话等弹层，焦点位于屏幕外。
- 在 `app/closet.py` 为关闭的 sheet/sidebar 增加 `visibility: hidden`，保留原有打开机制。
- 修复后连续 16 次 Tab 没有进入关闭弹层；打开上传弹层后按钮可见、可聚焦，关闭操作正常。
- 390、393、430、1440 宽度无水平溢出；浏览器未捕获 pageerror 或 HTTP >=400。仅代表本次路径。
- 自动化回归 7 项通过，见 `regression.xml`。该静态防回归用例不能替代真实浏览器结果。

## 证据与边界

`browser-results.json` 包含修复前后焦点坐标、宽度检查及运行时 closet.py SHA256；`after-*.png` 为修复后截图。`upload-open-after.png` 捕获在进入动画开始时，不用于证明弹层完成展示；使用 `upload-open-settled.png`。

未完成：完整 E2E-012、所有人格成功路径、10 分钟稳定性、10 次往返、正式 HTTP 性能验收、内容四道门和独立盲审。所有相关正式用例仍不得据此标记 Pass。
