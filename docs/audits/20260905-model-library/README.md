# 模特主数据接入

来源：项目现有 `tests/fixtures/tryon_models/manifest.json` 及同目录原始照片。当前六条启用主数据，名称、性别、体型、排序均直接读取；没有另建硬编码名单。

- GET `/selfit/try-on/models`：返回启用且原图存在的模特，按主数据排序，图片带文件版本指纹。
- PUT `/selfit/try-on/model`：验证当前启用的模特 ID 后保存到现有 `current_model_id` 偏好；也支持 `self`。无效/停用模特拒绝保存。
- 选择器展示主数据照片、名称、体型与选中态；我的照片单独保留。
- 选择预设模特时清除上次个人文件引用及旧试穿结果；生成预检与任务使用所选模特原图和正确文件名。
- 刷新恢复保存的模特 ID；新上传个人照片切换到 self，个人照片与库中模特可互相切换。

## 验证

`tests/test_selfit_studio.py`：5 passed，含主数据排序、停用/缺图过滤、身份验证、选择持久化和无效 ID 拒绝。JS/Python 语法检查通过。

实际浏览器：六位模特成功展示；选中沙漏型后主图地址为 `/tryon-models/female_medium_1.png`，刷新后不变；当前套装预检成功。未为这次选择变更额外调用生图。验收后已恢复原先的“我的照片”选择，并打开模特选择器供使用。

393 × 852 / 430 × 932 检查：页面及弹窗均无横向溢出，6 张模特照片无加载错误，控制台 error/warn 为空。弹窗纵向滚动可浏览完整模特列表。

本地服务已重启生效，未部署线上。
