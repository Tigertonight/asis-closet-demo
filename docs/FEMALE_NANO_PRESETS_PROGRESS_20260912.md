# 新女模特整套预设：2026-09-12 恢复进度

用户当前指令为“完成新女模特的试穿预设”。继续原 96 套 × 3 位新女模特批次，共 288 张；原七牛 selfit 私有空间上传与登记授权继续有效。

## 当前执行

- 使用本地 ADC → Vertex → `gemini-3.1-flash-image`，一整套一次生成。
- 恢复前校验 671 个输入文件和 96 份 catalog，当前源套装与快照一致；三位模特仍使用原定 SHA 对应的新 PNG。
- 原用户暂停文件已归档至 `resume-20260912/STOP.user-pause-20260911.txt`。恢复依据与校验回执见同目录 `resume-receipt.json`。
- 2 并发出现较多 HTTP 429 后，已让在途请求结束并切回单并发。调节并发的临时 STOP 已归档为 `STOP.capacity-switch-01.txt`，不是新的用户暂停。
- 2026-09-13 05:22（北京时间）已登记 **249/288** 张：匀称 85、纤细 80、丰满 84。第 15 轮已结束，无在途生成或待目检图片。剩余 33 张为接口失败 11、目检退回 18、数值失败 4，另 6 张因服务端审核问题暂挂。快照为 `checkpoint-249.json`，逐项待办见 `resume-20260912/REMAINING-249.md`。第 16 轮尚未启动：自动审批拦截了向 Google Vertex/Nano Banana 传输原模特及服装参考图的调用，待用户明确确认该数据传输；已设置技术 STOP 防止误启动，详见 `run16-approval-hold.json`。原七牛上传授权继续有效。
- 每个结果必须通过原 28/18 数值阈值及逐张目检，再私有上传、回读核对哈希并登记。失败请求与不合格图片全部保留。

批次目录为 `outputs/tryon-examples/female-nano-one-shot-20260911/`。`resume-20260912/` 保存本次运行日志、逐张验收 JSON、上传日志和验证结果。9 月 11 日 `handoff/` 是历史暂停快照，不能用于判断当前任务是否完成。

## 断点与重试

`--max-attempts` 是累计尝试上限；新增的 `--max-additional-attempts` 限定本轮每个任务新增请求数，避免为了重试旧任务而给新任务开放过多自动重试。已生成待审、已验收、已上传或内容审核阻止的任务均跳过。STOP 与运行锁仍生效。

```bash
.venv/bin/python scripts/batch_female_nano_presets.py status
.venv/bin/python scripts/review_female_nano_presets.py pending
# 当前技术 STOP：先取得向 Google Vertex/Nano Banana 传输原模特与服装参考图的明确确认。
# 确认后归档本次 STOP、确认无其他运行者，再启动以下有限重试；不是自动恢复授权。
.venv/bin/python scripts/batch_female_nano_presets.py run --concurrency 1 --max-attempts 10 --max-additional-attempts 1 --limit 128 --api-first
```

个别反复改变脸部的任务可启用 `useIdentityDetail`：在同一次整套请求中追加同一张原始模特照片的无损面部裁剪参考，记录来源 SHA 和裁剪范围，仍不超过 14 张输入。该参考只作用于输入，生成结果不拼脸、不修补。原图不上传七牛或 Git。

第 14 轮对目检发现发型偏移的任务启用 `useIdentityHairDetail` 无损头部与发束参考，固定裁剪原图 `(690,80,1140,700)`，保留完整头部及两侧肩前原发束。三张裁剪输入均已完整查看并与原图像素核对，记录见 `identity-hair-reference/preflight.json`；10 项已有恢复/提示词测试通过（`hair-reference-tests-01.log`）。仅补充同次请求输入，不修改输出，也不放宽质量检查。第 13 轮在途请求结束后停止，第 14 轮已加载此功能；25 项任务调整见 `pre-run14-adjustments.json`。

第 15 轮将试用三个 `retryOutfitReference`：FLOU 02 丰满第 7 次、WABI-curvy 02 匀称第 3 次、通勤 03 纤细第 2 次。它们均已完整目检且只有明确的服装细节问题，数值已通过。仅把这些未修改的旧结果作为新请求 Image 2 整套参考，Image 1 仍是原模特，全部单品拆图及原模特细节参考保留；新结果仍从 Nano 完整生成并重新双重验收，旧失败不能直接选入索引。输入校验要求同一 job、同一模特/套装、精确 SHA、数值通过及目检失败回执，不允许审核阻止项或跨套装引用。13 项相关检查通过，见 `reviewed-reference-tests-01.log`；试用列表及已知问题见 `reviewed-outfit-reference-pilot-01.json`。原失败与原搭配源文件均保持不变。FLOU 02 丰满第 8 次已成功补齐缺失圆环项链，单枚戒指和短门襟、原发束仍保留，经重新目检及数值检查通过并登记；见 `reviews-195.json` 和 `publish-126.log`。另外两个试用在第 15 轮返回 API 429，原候选参考与修正说明保留，待获准后重试。

根据上述成功试用，对 12 个已逐张完整查看、数值通过且仅剩明确细节问题的同任务候选配置了后续参考，记录见 `reviewed-outfit-reference-followup-02.json`（9 项）与 `reviewed-outfit-reference-followup-03.json`（3 项）。主要纠正重复戒指、围巾双前端、发束移到背后及开衫扣合方式；候选仍是失败图，不能直接登记。第 16 轮尚未执行，所有新配置仍待加载。

第 12 轮提示词补充同一配饰跨参考图不重复计数、单枚戒指仅一枚、未列出的手镯不复制，并保留明确成对/多件配饰与成衣自带饰件。10 项现有提示词恢复测试通过（`reference-count-tests-01.log`），已在 `run-12.log` 对应运行启用。

第 13 轮起，通用提示词明确正常外套必须双臂穿入袖子，并随原持机臂/垂臂弯曲；仅单品明确披肩穿法时允许空袖。另明确原手机背面不添加小人、屏幕或贴纸。`sleeve-prompt-tests-01.log` 的 10 项相关检查通过。第 12 轮已结束；第 13 轮已启用这项补充。

新增可选 `--api-first`，第 12 轮优先补接口失败任务，保持同类原顺序，累计上限、单轮上限、STOP 和待审/已登记跳过规则不变。10 项提示词与恢复测试通过（`api-priority-tests-01.log`）。第 12 轮已启用该顺序。

服务端 HTTP 429 现在触发跨任务共享冷却，依次等待 60/120/240 秒、最多 300 秒，状态保存在 `rate-limit.json`。新增派发间隔至少 35 秒，依据近期每分钟第 3 次请求反复返回 429 的日志采取平滑限速；这是本批实测策略，并非声称 Google 固定官方配额。9 项恢复/提示词检查通过（`dispatch-spacing-tests-01.log`）。新任务也必须等待，避免单任务退避却让后续任务连续碰限流；STOP 可中断冷却。

旧失败任务的单任务退避现在从上次响应结束时间计算，已经过去的等待时间不重复等待；实时 429 共享冷却仍完整保留。STOP 在等待期间每 2 秒检查。8 项提示词/恢复测试通过（`retry-backoff-tests-01.log`）；本轮技术停派发回执 `STOP.elapsed-backoff-03.txt` 已归档，任务继续。

纤细 EASE 01 可选 `styleCropBox`，仅把原搭配参考裁至衣物范围，完整单品参考和清单仍保留。每次请求记录原始参考 SHA、裁剪范围及派生图 SHA，避免复制来源人物发型；原始输入和旧尝试不改动。

纤细 EASE 01 的第 8 次结果已通过全部 14 件单品、原肩前长发及数值保护检查，完成私有上传。FILM-curvy 02 白帽的丰满版第 6 次通过（面部差异 26.98）并上传；匀称版同次结果把帽子戴反，虽然数值通过仍在逐张目检中退回，纤细版仍超过数值阈值。下一次明确要求帽檐向前略上抬、后调节带不得出现在额头，保留完整原额头；没有放宽检查或修改结果图。LOOP 02 牛仔钟形帽现已补齐三个体型；丰满版补充原始身份细节参考后通过并上传。

## 源资料冲突记录

ICED 04 灰紫针织的旧 `wearing_method` / `closure_state` 标记闭合，但实际原笔记、单品图及当前详细描述均为低 V 领敞开式开衫，内有同色 U 领罗纹内搭。本批依据实际图像验收敞开穿法，冲突写入 `reviews-38.json`。三个体型均已通过并上传；纤细重做后恢复原微笑及正确针织结构。没有修改源 catalog 或将冲突隐藏。

FLOU 02 薄荷蓝粉上衣的名称虽为“开衫”，实际单品图为胸前短门襟、腹部连续针织结构；绿色宝石戒指为单枚。重试明确保留短门襟、不塞衣摆、不复制源手机侧绿色细节为多枚戒指，衣物参考采用已查看原图的局部裁剪，完整单品参考仍保留。细节见 `reviews-44/50/51.json` 与 `flou02-refinement-02.json`。

第 14 轮 FLOU 02 匀称版恢复原发束后仍出现全长开衫，追溯发现旧结构化穿法也与短胸门襟实图冲突。新增仅限请求的 `itemContextOverrides`，需要现有单品名称和明确图像依据，只能修正穿法/结构说明，不能变更单品引用、身份、输入文件或原 catalog。三体型已记录相同短门襟纠正，下一轮加载；`item-context-correction-tests-01.log` 的 12 项检查通过。证据为 `flou02-request-context-correction-01.json`，原图、源资料、失败结果均保留。

第 14 轮验收后进一步明确了单枚戒指数量与位置：FLOU 02、通勤 04 仍按持机手一枚；OOPS 04 原资料未固定手指，改为原垂手无名指一枚、持机手完全裸露，减少重复。证据及原单品依据见 `ring-context-correction-01.json`。WABI-curvy 02 原资料的上部单扣合拢在结果中反复变成全开，补充明确的单扣连接说明，并完整查看去除源人物头部的衣物裁剪参考；见 `wabi-curvy02-request-adjustment-01.json`。这些请求级修正从第 15 轮加载，源文件、单品 ID 和原 catalog 不变。

VOID 03 单品名称为“浅灰罗纹堆堆袜”，实际单品图为两端开口的袜套，原搭配露脚掌与脚趾。本批按实图验收；纤细已登记结果穿法正确，匀称第 2 次另出现手机背面小人图案，已退回重做并补充袜套说明。见 `reviews-126.json` 及当前 job 的重试说明。

VOID 02 的原搭配要求橄榄绿开衫下前襟敞开，单品陈列图扣合状态容易被误当穿法。完整查看原搭配后，为剩余匀称/纤细任务补充仅上部两扣闭合、下部全开及最小米白基础内搭的请求级说明，见 `void02-request-context-correction-01.json`；保留源 catalog 与所有原单品。

VOID-curvy 04 头巾纤细/丰满第 4 次仍超过原面部阈值，已完整查看且保留失败，观察记录为 `headscarf-failed-quality-observations-04.json`。度假 01 纤细/丰满第 3 次同样数值失败；头巾不适用戴帽复核规则，不能通过修改阈值或把失败图直接登记解决。

## 戴帽面部检查修正

检查发现原 `_face_region_difference` 在脸框外额外扩展 18%，把帽子覆盖的头发及变化的衣领也纳入“面部差异”。原脸框完整区域仍保留眼、鼻、嘴及下颌。对于明确戴在头上的帽子、且唯一失败项为面部差异的图片，本批采用 `worn_hat_unexpanded_face_box_v1` 复核完整原脸框，**阈值仍是 28，背景阈值仍是 18**。未戴的草帽、墨镜、发圈和背景失败不使用这项规则。NOIR 03 的明确穿法“灰色连帽罩在头部”现也由同一规则识别；未罩、不罩、没有罩或垂在背后的连帽不适用。新增正反例和面部仍变更的失败测试，没有降低数值阈值。纤细 NOIR 03 保留原失败报告，原脸框复核为 26.02，经过逐张目检后才登记。

只新增检测记录，不修改生成图、不删除原失败报告。旧图复核使用 `scripts/recheck_female_nano_headwear.py`，新增 `quality-recheck-headwear.v1.json`，以 `qualityReportPath` 绑定，并记录原报告 SHA；原 attempt 状态仍保留为失败。发布器使用实际原图/结果重算复核数值后，才接受对应逐张目检通过记录。误差仍超标的白帽组合维持失败。

验证证据在 `resume-20260912/hat-face-roi-diagnostic.png`、`headwear-recheck-01.json`、`preflight-headwear.log` 与 `regression-tests-02.log`；此前相关 37 项回归测试通过；新增派发间隔及连帽语义测试后，`regression-tests-03.log` 的 40 项相关回归测试通过。`verify-12.log` 已验证全部当前 249 条女模特结果绑定及 16 条男模特保留；`matching-10.log` 的 249 条真实命中和三类负向检查全部通过。所有当前已验收结果均完成上传回读核验；剩余 39 张尚未完成，不能执行全批 finalize。

## 固定模特戴帽五官检查补充（2026-09-13）

进一步查看白帽第 8 次原图/结果叠框（`white-cap-face-box-08.png`）发现，v1 原脸框上方 45 像素仍可能被正确正戴的帽檐覆盖。对三张已绑定 SHA 的 1792×2400 原模特逐一检查后，预先固定 `(834,270,216,171)` 区域完整保留双眉、眼、鼻、嘴和下颌，示意见 `fixed-model-feature-box.png`。新增显式启用的 `worn_hat_fixed_visible_features_v2`：仅明确戴帽且唯一失败项为面部时，先保留原 v1 计算；v1 仍失败才检查该固定五官范围，阈值仍为面部 28、背景 18。区域由原模特定义，不能根据结果图动态挑选；墨镜、发带、头巾和未戴帽不适用。

旧 v1 回执按原规则复算。v2 单独写 `quality-recheck-headwear.v2.json`，保存原扩展脸框、原完整脸框分数、被排除的额头范围和初始报告 SHA，原图与失败报告保持不变。发布时根据具体规则从原照片重新计算，再检查逐张目检回执。数值通过仍会因多余配饰等问题退回，例如 LOOP 03 纤细版第 3 次多出黄色戒指。

`feature-face-tests-02.log` 的 17 项检查通过，包含额头遮挡、眉/眼/鼻/嘴/下颌变动拒绝、背景变动拒绝、旧规则兼容、错误画布/脸框和非帽配饰保护；`feature-preset-tests-01.log` 的 27 项预设检查通过。白帽匀称/纤细第 8 次及 FLOU 01 丰满第 7 次经复核和完整目检后上传登记；全批生成仍在继续。第 12 轮已结束；第 13 轮已启用 v2。旧请求可用显式复核命令处理，不能与同一 job 的生成同时改写。

## 服务端审核拦截

约会 04「黑白格蕾丝套装」纤细/丰满，以及度假 04「橙色泳装叠穿」匀称/纤细，第 1 次返回 `IMAGE_SAFETY`，没有结果图。保存完整服务端响应，未自动重试或改变审核设置。另两个同款体型上次为 API 失败，因同套审核问题暂挂 `moderationHold`（`directProviderBlock=false`），明确区别于实际审核拒绝。暂缺共 6 张，不能计为完成。

已向用户询问约会 04 的保留缺项或更换安全款式偏好，尚待回复。其余 33 个缺项已整理好后续修正，但第 16 轮因数据传输自动审批拦截而暂停，等待明确授权。详情见 `pre-run13-adjustments.json`，第 13 轮每任务最多新增 1 次尝试、单并发、累计上限 8，已登记及审核拦截项全部跳过。

## 收尾待办

- [x] 恢复输入哈希与源数据一致性检查。
- [x] 截至最近完整检查的 249 个实际登记预设的命中与错误照片/个人照片/部分单品拒绝验证通过；证据为 `resume-20260912/actual-preset-matching.json`（全量完成后更新）。
- [x] 本轮请求预算、STOP 防护及无损身份参考共 5 项测试通过。
- [ ] 288 张逐张验收、私有上传并登记。
- [ ] 检查全部当前源、全部单品 ID、模型与结果 SHA 绑定、上传回执及缓存。
- [ ] 288 条实际预设命中及负向拒绝验证；运行相关回归测试。
- [ ] 保留原 16 条男模特数据，执行 `finalize` 后总计 304 条，更新最终统计与交接清单。

后续用户已要求“之后转化为webp，commit并push”。提交本批已验收预设、WebP 显示引用、相关脚本/验证与进度记录；其他工作区改动保持原样。凭据、原模特照片、输入参考及过程目录不纳入 Git。当前没有服务器部署指令。

## 无损 WebP 显示版本（2026-09-13）

用户要求将预设转为 WebP 并 commit/push。对当前 249 张已验收、已上传的 PNG 生成原尺寸无损 WebP，保留全部像素（含透明像素的 RGB）并逐张解码比对。PNG 的结果 SHA、生成请求、数值检查及逐张目检证据均不修改；新增 `displayResult`，绑定原 PNG SHA、WebP SHA、尺寸与七牛上传回读回执。

运行时仍先通过原预设的模特、完整套装与验收检查，再选择来源 SHA 一致、已核验且声明像素相同的 WebP；显示记录缺失、失效或不匹配时回退原 PNG。其他照片、个人照片及部分单品不能凭 WebP 记录跳过预设检查。

```bash
# 只读取已验收结果，本地转码，不调用 Google：
.venv/bin/python scripts/convert_female_nano_presets_webp.py prepare
# 七牛 selfit 私有空间上传与登记使用既有授权：
.venv/bin/python scripts/convert_female_nano_presets_webp.py publish
.venv/bin/python scripts/convert_female_nano_presets_webp.py verify
```

本地编码清单与逐张回执在批次 `webp-display/`，失败或未验收的 39 个缺项不参与。后续补齐新预设后重新 prepare/publish，保留历次索引快照。转换不解除 Google 图像传输的技术 STOP。

当前 249 张已完成本地编码与全部 RGBA 像素比对：PNG 总计 1,126,975,938 字节，WebP 总计 783,575,008 字节，减少 30.47%。证据为 `webp-display/manifest.json` 和 `resume-20260912/webp-prepare.log`。本次相关测试 57 项及新增 WebP 历史回归 1 项通过（共 58 项），见 `webp-tests.log`、`webp-history-test.log`。

2026-09-13 05:36（北京时间）249 张 WebP 已全部上传七牛 selfit 私有空间并回读验证哈希、MIME；索引与 job 的显示引用一致。`webp-display/verification.json` 确认全部像素相同、原生成/验收证据及其他预设保持不变，249 条实际命中均返回 WebP，换照片/个人照片/部分单品负向检查全部通过。本次转换与上传完成，生成批次仍为 249/288，剩余 39 张继续保持前述暂停状态。
