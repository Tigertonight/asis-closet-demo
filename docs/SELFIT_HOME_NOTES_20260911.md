# 试衣镜首页四篇笔记（2026-09-11）

普通试衣镜入口调用 `GET /selfit/try-on/report-outfits/home`，服务端按当前登录账号的最新有效报告选择内容。客户端不额外获取档案或照片；首页仍只需偏好、模特、笔记三个数据请求。

- 未测试：按现有账号性别筛选，随机选取四张不同笔记原图，保留已选笔记。同一页面内导航保持列表；刷新重新抽取。
- 已测试：使用最新报告的人格和模板（含男女/微胖版本），返回四篇实际交付笔记。报告保存了完整顺序时沿用该顺序；旧报告没有卡片数据时使用对应模板的四篇。保留原图、单品、穿法和叠穿映射。
- 重新测试产生新报告后，下次进入/刷新使用新报告；其他账号的报告和没有有效人格的草稿不参与选择。
- 已有报告的列表不会因为先前选择了其他套装而挤入第五篇或替换某篇。用户明确打开的当前试穿套装仍按现有规则保留。
- 显式从报告进入时，保留原来的报告笔记参数与顺序；衣帽间仍展示用户自己的套装。
- 首页响应需登录，使用 `Cache-Control: private, no-store`。保留 `/random` 兼容接口，数量同步改为四篇。

若人格/模板缺少完整四篇或对应交付素材，显示恢复提示。沿用现有男版报告的素材边界：尚未提供男版内容的人格不会被替换成女版或其他人格的随机笔记。本次没有新增报告素材。

## 验证

- `node --test tests/test_selfit_home_loading.cjs tests/test_selfit_home_notes.cjs tests/test_selfit_report_outfits.cjs`：15 项通过。
- `python -m pytest -q tests/test_selfit_home_notes.py tests/test_selfit_report_outfits.py tests/test_male_styling_delivery.py tests/test_tryon_rate_limits.py`：29 项通过、16 个子检查通过；仅有现有 Starlette 弃用提示。
- JS 语法检查、`git diff --check`：通过。
- 浏览器使用独立 localhost 服务、合成未测试/FLOU/VOID 微胖账号、真实首页接口和生产 JS/CSS，以及现有本地素材缓存，不修改真实账号数据。
- 393 × 852：未测试账号恰好四篇，刷新后重抽；图片全部加载。FLOU 为「浪漫彩搭、薄荷蓝粉、vintage浪漫、斗篷上衣」，刷新顺序不变。
- VOID 微胖为「柔性静灰、格纹叠穿、遮胯长裤、印花对拼」，可横向翻到第四篇。
- 1440 × 960：393px 应用居中；手机和桌面没有页面横向溢出、文字重叠、裁切按钮或控制台错误。
- DESIGN.md 本次相关检查：图片主导、加载/恢复提示、试穿入口、移动与桌面布局均保持正常。

本次未提交、推送或部署。工作前文件快照、隔离验证服务和测试日志保存在 `outputs/home-notes-20260911/`。
