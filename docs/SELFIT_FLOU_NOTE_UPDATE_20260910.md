# FLOU 两条笔记更新（2026-09-10）

FLOU 报告的 `outfits-01` 和 `outfits-02` 已使用用户提供的新拆款替换，报告、灵感库和试穿入口共享当前素材绑定。

| 位置 | 原笔记 | 新笔记 | 作者 | 单品数 |
| --- | --- | --- | --- | --- |
| outfits-01 | 薄荷绑带 | 浪漫彩搭 | @羅蛋皮 | 7 → 11 |
| outfits-02 | 撞色叠纱 | 薄荷蓝粉 | @Moromoro | 7 → 10 |

作者和链接由用户在本次对话提供：[浪漫彩搭原笔记](https://www.xiaohongshu.com/discovery/item/66b9c74c000000001e01b534)、[薄荷蓝粉原笔记](https://www.xiaohongshu.com/discovery/item/68eb992f000000000703a89e)。实际报告字段保留用户提供的完整分享链接。其他 78 条报告笔记、人格基础信息和候选库保持原有内容。

## 素材与描述

- 输入包：`FLOU_造梦浪漫型人格-两套拆款.zip`，SHA-256 为 `913a9385b09bf8b436d50465452945b7f32bca053e470b3be58d1fbb91f5aa4d`。
- 21 张单品全部已有透明通道。使用既有 `cutout_batch.py` 的 `existing_alpha_preserved` 流程，逐字节保留图片，输出蒙版、预览及处理记录；没有重新分割或重绘。
- 逐件查看浅灰底与深色底预览，检查白色蕾丝、细链、珠饰、金属高光和细小配饰。21 张处理成功，0 张失败。
- 基于原穿搭图及全部拆件图补充 21 段单品描述、2 段整套描述。被遮挡的原物结构存在近似补全，描述保留其证据边界；第二套上衣的拆件图为半门襟，已说明其与资料中“开衫”穿法的结构差异。
- 按资料明确的覆盖关系规范化叠穿顺序：连裤袜位于半裙内，长裙片覆盖部分靴筒，内搭位于外层上衣内；补齐关联单品 ID，保留原始顺序供追溯。
- 用户已明确授权向项目现有七牛 `selfit` 私有空间上传。21 张单品与 2 张原图使用内容哈希资产 ID，全部经远端回读、SHA-256、字节数和 PNG 解码校验。
- 新增素材的 Git 记录只保存对象地址、存储定位和校验信息，不包含临时下载签名。普通读取沿用项目的已发布链接或已校验缓存；首次在无缓存环境使用这些素材时，维护者须先刷新运行环境中的私有素材清单。可将素材主表复制到被 Git 忽略的 `outputs/material-assets.runtime.json`，执行 `python scripts/refresh_material_urls.py --registry outputs/material-assets.runtime.json`，并在启动服务时设置 `SELFIT_MATERIAL_REGISTRY_PATH` 指向该文件；刷新后的清单不提交 Git。

## 替换与检查

更新了拆款交付、原图清单、透明图清单、报告图片清单、素材注册表、报告编辑器主数据及 JS 种子、运行时报告 JSON 及 JS。素材内容采用独立版本 ID；旧对象保留，旧套装 ID 返回 404，旧原图 ID 请求返回 409。

旧批量试穿记录作为历史数据保留。对这两条笔记的 6 条旧示例逐条检查，当前来源图和单品 ID 校验均拒绝复用；新笔记走正常实时生成路径，本次没有重新生成 6 张固定模特示例。

验证结果：

- `test_report_note_sync.py`、`test_selfit_report_outfits.py`、`test_selfit_tryon_presets.py`、`test_saved_styling_assets.py`、`test_inspiration_delivery.py`：38 项通过。
- 两条新笔记接口分别返回 11、10 个单品，试穿计划能解析全部 21 个素材；旧套装、旧原图和旧预生成结果隔离检查通过。
- 两份原图/报告清单共 711 张图片通过下载或本地缓存完整性检查；本次新增 23 张独立完成远端回读。
- 本地浏览器在 393px 手机宽度及 1440px 桌面宽度检查新封面、单品、底部操作及居中布局；图片成功加载，无横向溢出，控制台无资源错误。

原 96 套导出文件夹同步为 571 张单品 PNG 和对应 TXT：447 份既有完整描述、21 份本次看图补充描述、103 份原拆款字段说明。过程文件与旧版本备份位于 `outputs/flou-note-update-20260910/`。

本次完成本地项目替换及七牛素材上传，尚未执行服务器代码发布。服务器发布仍须先提交并推送，再在服务器运行 `sudo bash scripts/deploy_release.sh [commit]` 并核对 `/health` 指纹。
