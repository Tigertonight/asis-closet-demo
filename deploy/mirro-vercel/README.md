# Mirro on Vercel

Standalone static deployment of the selfit smart-mirror capture experience.

- `/` opens the mirror experience.
- `/selfit/mirror` preserves the local product route.
- Camera access is browser-side and requires HTTPS; Vercel provides HTTPS.
- The current deployment uses the existing demo result and QR asset. Real analysis remains a separate backend integration.
